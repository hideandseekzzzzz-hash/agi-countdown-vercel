{
  "name": "silicon-specimen",
  "version": "1.0.0",
  "description": "Silicon Specimen official site",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "vercel-build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "npm --prefix frontend run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
